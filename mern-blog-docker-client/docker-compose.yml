version: '3.8'
services:
  api:
    build: .
    container_name: mern-blog-client-frontend
    ports:
      - '2002:3000'
    volumes:
      - /app
      - /app/node_modules
    stdin_open: true
    tty: true 